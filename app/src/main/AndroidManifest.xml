<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="io.heckel.ntfy" />
    </queries>

    <application
        android:name=".ClipShiftApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.ClipShift">

        <service
            android:name=".ClipShiftService"
            android:foregroundServiceType="remoteMessaging"
            android:exported="false" />

        <activity
            android:name=".ClipboardSendActivity"
            android:theme="@style/Theme.ClipShift.Transparent"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:exported="false" />

        <activity
            android:name=".ClipboardWriteActivity"
            android:theme="@style/Theme.ClipShift.Transparent"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:exported="false" />

        <activity
            android:name=".ShareActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".NtfyReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.heckel.ntfy.MESSAGE_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
